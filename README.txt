AutoCodeLoader

This project will automatically generate Factories, Proxies and Setter Traits
to avoid you having to duplicate this boilerplate constantly.

Currently its scope is quite limited. It will only generate Factories designed
to work with the Aura.Di dependency injection container.
